# ============================================
# CHRONODIL APP - VARIABLES D'ENVIRONNEMENT
# ============================================

# ------------------------------------------
# DATABASE (Supabase PostgreSQL)
# ------------------------------------------
# IMPORTANT: Pour Vercel, utiliser Connection Pooling (port 6543)
# Supabase Dashboard → Settings → Database → Connection Pooling → Session Mode
DATABASE_URL="postgresql://USER:PASSWORD@HOST:6543/DATABASE?pgbouncer=true&connection_limit=1"
DIRECT_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE"

# ------------------------------------------
# SUPABASE
# ------------------------------------------
# Supabase Dashboard → Settings → API
NEXT_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"
SUPABASE_JWT_SECRET="your-jwt-secret"

# ------------------------------------------
# BETTER AUTH
# ------------------------------------------
# Générer avec: openssl rand -base64 32
BETTER_AUTH_SECRET="your-random-secret-32-chars"

# Pour production Vercel, mettre l'URL de production
BETTER_AUTH_URL="https://chronodil-app.vercel.app"

# ------------------------------------------
# APP
# ------------------------------------------
NEXT_PUBLIC_APP_URL="https://chronodil-app.vercel.app"
NODE_ENV="production"

# ------------------------------------------
# EMAIL (Resend)
# ------------------------------------------
# Resend Dashboard → API Keys
RESEND_API_KEY="re_xxxxx"

# ============================================
# INSTRUCTIONS
# ============================================
#
# 1. DATABASE_URL pour Vercel:
#    - Port: 6543 (pas 5432!)
#    - Paramètres: ?pgbouncer=true&connection_limit=1
#
# 2. DIRECT_URL pour migrations locales:
#    - Port: 5432
#    - Sans pgbouncer
#
# 3. Pour générer BETTER_AUTH_SECRET:
#    openssl rand -base64 32
#
# 4. BETTER_AUTH_URL et NEXT_PUBLIC_APP_URL:
#    - Development: http://localhost:3000
#    - Production: https://your-app.vercel.app
#
# ============================================
